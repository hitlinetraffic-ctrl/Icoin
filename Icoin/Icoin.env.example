# Скопируй в .env и замени значения.
JWT_SECRET=change-me
API_PASSWORD=change-me
REDIS_PASSWORD=change-me

# ВАЖНО: GENESIS_PRIVATE_KEY_B64 должен быть одинаковым на всех нодах.
# Генерировать (локально):
#   python -c "import base64; from Crypto.PublicKey import ECC; k=ECC.generate(curve='P-256'); print(base64.b64encode(k.export_key(format='PEM').encode()).decode())"
GENESIS_PRIVATE_KEY_B64=PASTE_BASE64_PEM_HERE
